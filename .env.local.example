# Gmail credentials for the contact form
# Step 1: Go to https://myaccount.google.com/apppasswords
# Step 2: Create an App Password (select "Mail" + "Other")
# Step 3: Paste the 16-character code as GMAIL_APP_PASSWORD (no spaces)

GMAIL_USER=sadethimira1234@gmail.com
GMAIL_APP_PASSWORD=your_16_char_app_password_here

# Optional: GitHub token to avoid API rate limits on build (60 req/hr unauthenticated → 5000 req/hr with token)
# Create at https://github.com/settings/tokens/new (no scopes needed for public repos)
# GITHUB_TOKEN=your_github_token_here
